# Environment: use "prod" for production (Gemini API), "local" (or anything else) for local LLM
NODE_ENV=local

# GitHub OAuth (for user login and repo access)
GITHUB_ID=your_github_client_id
GITHUB_SECRET=your_github_client_secret

# NextAuth Configuration
NEXTAUTH_SECRET=generate_a_strong_random_secret
NEXTAUTH_URL=http://localhost:3000

# WebSocket URL (for Docker deployment, use your EC2 IP)
NEXT_PUBLIC_WS_URL=ws://localhost:4001

# Google Cloud Platform (Cloud Run Deployment)
GCP_PROJECT_ID=your_gcp_project_id
GCP_SERVICE_ACCOUNT_KEY=your_service_account_json_as_string_or_file_path

# Firebase Configuration (used for Firestore, Auth, etc.)
FIREBASE_SERVICE_ACCOUNT_KEY=base64_encoded_json_key_here
FIREBASE_API_KEY=your_firebase_api_key
FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
FIREBASE_PROJECT_ID=your_firebase_project_id
FIREBASE_STORAGE_BUCKET=your_project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=your_sender_id
FIREBASE_APP_ID=your_app_id
FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# LLM: Smart Project Scan
# - When NODE_ENV=prod: Amazon Bedrock (Claude Opus 4.5) is used (requires AWS credentials below).
# - When NODE_ENV=local: Local LLM via OpenAI-compatible API (e.g. Ollama, LM Studio).
# Local LLM (only when NODE_ENV is not "prod")
# LOCAL_LLM_BASE_URL=http://localhost:11434/v1
# LOCAL_LLM_MODEL=llama3.2

# AWS Configuration (for AWS deployments - Elastic Beanstalk, ECS, EC2, and Bedrock LLM)
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
# Optional: Override Bedrock model ID (default: anthropic.claude-opus-4-5-v1:0)
# BEDROCK_MODEL_ID=anthropic.claude-opus-4-5-v1:0
# Note: AWS credentials are also used for Claude Opus 4.5 on Bedrock when NODE_ENV=prod
# Optional: Elastic Beanstalk EC2 instance profile and service role (defaults below)
# AWS_EB_INSTANCE_PROFILE=aws-elasticbeanstalk-ec2-role
# AWS_EB_SERVICE_ROLE=aws-elasticbeanstalk-service-role
# Optional: ECS ALB HTTPS - ACM certificate ARN (same region as ALB). When set, HTTPS on 443 and HTTP→HTTPS redirect are enabled. No user-facing option; server-side only.
# ECS_ACM_CERTIFICATE_ARN=arn:aws:acm:us-west-2:123456789012:certificate/...
