const config = {
	NEXT_PUBLIC_DEPLOYMENT_DOMAIN: process.env.NEXT_PUBLIC_DEPLOYMENT_DOMAIN || "smart-deploy.xyz",
	NEXT_PUBLIC_VERCEL_DOMAIN: process.env.NEXT_PUBLIC_VERCEL_DOMAIN || "",
};

export default config;
